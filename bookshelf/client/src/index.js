import React from 'react';
import ReactDOM from 'react-dom';
// import './index.css';
import App from './App';
require("dotenv").config(); //do I need to change this or only have in server?

ReactDOM.render(<App />, document.getElementById('root'));
